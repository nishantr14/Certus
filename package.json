{
  "name": "certusdoc-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"uvicorn api:app --host 0.0.0.0 --port 8000\" \"npm run dev --prefix landing\"",
    "backend": "uvicorn api:app --host 0.0.0.0 --port 8000",
    "frontend": "npm run dev --prefix landing"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
